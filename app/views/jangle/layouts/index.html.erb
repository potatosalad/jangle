<%= link_to content_tag(:span, 'Create New Layout'), new_jangle_layout_path, :class => 'big button' %>
<h1>Layouts</h1>

<ul class='list'>
  <%= render :partial => 'index_branch', :collection => @layouts %>
</ul>